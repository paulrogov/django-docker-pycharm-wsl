# Django, docker-compose, PyCharm inside WSL

Single container:
* `django-docker-pycharm-wsl` (hello world django application)

## Commands
Start project (http://localhost:8000/)
```bash
docker-compose up -d
```

Logs
```bash
docker-compose logs -f
```
